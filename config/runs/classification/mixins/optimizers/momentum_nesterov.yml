training:
  optimizer:
    class_name: sgd
    config:
      learning_rate: 0.1
      momentum: 0.9
      nesterov: true
  finetune:
    optimizer:
      class_name: sgd
      config:
        learning_rate: 0.001
        momentum: 0.9
        nesterov: true
